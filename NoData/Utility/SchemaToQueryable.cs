using NoData.GraphImplementations;

namespace NoData.Utility
{
    public class SchemaToQueryable
    {
        public static void Test()
        {
            var treeSchema = new GraphImplementations.Schema.Tree(null);
            var subGraphSchema = new GraphImplementations.Schema.SubGraphSchema(null, null);
            
        }
    }
}